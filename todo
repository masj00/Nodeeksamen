Add sockets end to end. Implement a feature that clearly uses socket.io (server + client) so you can demo real-time behavior.

(Socket.io now wraps Express and maintains a rolling “Study Room” message history, so any client can join and broadcast tips in real time (server/app.js (lines 1-108)). Each connection immediately receives the current history, and the server sanitizes/limits incoming payloads before emitting to everyone.

To support the new UI flow, the auth router exposes a logout endpoint that destroys the session and clears the cookie (server/routers/authRoutes.js (lines 120-129)).

The profile view is now the hub for real-time collaboration: after confirming the session it opens a socket.io-client connection, listens for history + live messages, and renders a chat feed plus send form; logout reuses the new API (client/src/pages/Profile.svelte (lines 1-120)).

Client dependencies include socket.io-client so remember to install/update packages (client/package.json (lines 16-21), run npm install inside client). Restart the server (npm run dev in server) so the new HTTP+Socket.IO setup takes effect, and run the Vite dev server to test chatting between two browsers.

I didn’t run automated tests (none defined); please exercise login + chat manually in two tabs to verify broadcasts reach everyone.)



Flesh out authorization beyond simple login. For example, admin-only routes/pages or role-based UI; enforce the same on the server.

Polish the UI/UX: replace the placeholder home page, style the login/signup/profile views, add clear flows and validation. Make sure you can demo several distinct user-facing features.

Remove dev-only logs/console output; ensure .env secrets aren’t committed. Add basic GDPR touches: cookie notice, clear data-handling info.

Document how to run the project (README) and test the DB access via terminal queries so you can demonstrate that during the exam.

Plan the demo: list 5–6 user-facing features you can show without diving into code, prep accounts (especially an already logged-in user), and make sure sockets/auth flows are smooth.



<script>
  import { Router, Link, Route } from "svelte-routing";
  import 'toastr/build/toastr.min.css';
  import Login from "./pages/Login.svelte";
  import Profile from "./pages/Profile.svelte";
  import StudyRoom from "./pages/StudyRoom.svelte";
  
</script>


<Router>
  <nav class="nav">
    <Link to='/'>Home</Link>
    <Link to='/login'>login</Link>
    <Link to='/profile'>profile</Link>
    <Link to='/study-room'>study room</Link>
  </nav>
  <div>
    <Route path='/'>
      <h1>
        Study Helper
      </h1>
      <h2><span>Coming Soon™</span></h2>
    </Route>
    <Route path='/login'><Login /></Route>
    <Route path='/profile'><Profile /></Route>
    <Route path='/study-room'><StudyRoom /></Route>
  </div>
</Router>





:root {
  font-family: system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;

  color-scheme: light dark;
  color: rgba(255, 255, 255, 0.87);
  background-color: #242424;

  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a {
  font-weight: 500;
  color: #646cff;
  text-decoration: inherit;
}
a:hover {
  color: #535bf2;
}

body {
  margin: 0;
  display: flex;
  place-items: center;
  min-width: 320px;
  min-height: 100vh;
}

h1 {
  font-size: 3.2em;
  line-height: 1.1;
}

.card {
  padding: 2em;
}

#app {
  max-width: 1280px;
  margin: 0 auto;
  padding: 2rem;
  text-align: center;
}

button {
  border-radius: 8px;
  border: 1px solid transparent;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  font-family: inherit;
  background-color: #1a1a1a;
  cursor: pointer;
  transition: border-color 0.25s;
}
button:hover {
  border-color: #646cff;
}
button:focus,
button:focus-visible {
  outline: 4px auto -webkit-focus-ring-color;
}

@media (prefers-color-scheme: light) {
  :root {
    color: #213547;
    background-color: #ffffff;
  }
  a:hover {
    color: #747bff;
  }
  button {
    background-color: #f9f9f9;
  }
}
